---
- hosts: kubernetes-master-nodes
  become: yes
  tasks:
  - name: Create external NodePort for Dashboard
    k8s:
      state: present
      definition:
        apiVersion: v1
        kind: Service
        metadata:
          labels:
            k8s-app: kubernetes-dashboard
          name: kubernetes-dashboard
          namespace: default
        spec:
          ports:
            - port: 443
              targetPort: 8443
          selector:
            k8s-app: kubernetes-dashboard
          type: NodePort
